---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json

# Run against the assembled image after identity changes and before signing.
type: script
snippets:
  - "/usr/libexec/barq-os/image-smoke-test --image"
