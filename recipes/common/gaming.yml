---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json

type: dnf
install:
  packages:
    - gamemode
    - mangohud
    - gamescope
    # Host integration for controllers, handhelds and Steam-related VR hardware.
    - steam-devices
    # Fedora 44 module autoload integration used by modern Wine/Proton paths.
    - ntsync-autoload
